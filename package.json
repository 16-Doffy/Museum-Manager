{
  "name": "museum-manage",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev:admin-portal": "turbo run dev --filter=admin-portal",
    "dev:mobile": "turbo run dev --filter=mobile",
    "dev:museum-portal": "turbo run dev --filter=museum-portal",
    "dev:visitor-portal": "turbo run dev --filter=visitor-portal",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "start": "turbo run start"
  },
  "devDependencies": {
    "turbo": "^2.5.8"
  },
  "packageManager": "pnpm@10.17.1"
}
