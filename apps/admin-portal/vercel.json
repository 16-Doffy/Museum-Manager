{
  "buildCommand": "cd ../.. && pnpm install && pnpm --filter admin-portal build",
  "outputDirectory": "dist",
  "framework": "vite",
  "installCommand": "cd ../.. && pnpm install",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
